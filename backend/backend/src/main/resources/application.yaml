spring:
  datasource:
    url: jdbc:mysql://localhost:3306/escuteasferas?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: root
    password: metal.666
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

jwt:
  secret: escuteasferas-chave-secreta-super-segura-2026-nao-compartilhe
  expiration: 86400000